// what is it supposed to contain ?
let EMAIL = "my.email@domain.com"
let TOKEN = "your_token_here"

func fetchUserId(email: String) async  -> String? {
    // FIXME: What is supposed to be here ?
    return nil;
}

enum EnvItem {
    case uat, prod
}

let ENV: EnvItem = .uat
